# Legacy Archive

This folder contains artifacts from the older script-based version of the project that existed before the Junction portfolio refactor.

Archived items:

- `frt_results.png`
- `mmash_attenuation.png`
- `mmash_processed.csv`

These files were moved out of the project root so the active repo stays focused on the package-based pipeline in `src/junction_portfolio/` and the current report outputs in `artifacts/`.
